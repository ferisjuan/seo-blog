{
  "scripts": {
    "start": "concurrently \"yarn --cwd frontend start\" \"sleep 2 && yarn --cwd backend start\""
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
